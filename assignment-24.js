//Write a function that takes an integer minutes and converts it to seconds.
// 1 minute = 60 seconds

var secondsConversion = function(minutes){
    return(minutes*60)
}

var minutes = 5;
console.log(secondsConversion(minutes));