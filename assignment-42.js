// We have an array of numbers, return true if all the elements
// inside the array are even numbers

const check = [2,4,6,8,10,'']

const finalArr = check.every((num) =>{
    return num % 2 === 0;
;})
console.log("🚀 ~ file: assignment.42.js ~ line 9 ~ finalArr ~ finalArr", finalArr)

